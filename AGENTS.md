# AGENTS.md

## 项目主体

本项目是 `SlimefunHelper-1.21.11`，一个面向 Minecraft Fabric 的客户端模组。

主要目标是为粘液科技游玩、普通生存、服务器探索和开发调试提供便利功能。

主要范围：

- Java 源码：`src/main/java/me/matl114`
- Fabric 资源与配置：`src/main/resources`
- 语言文件：`src/main/resources/assets/slimefunhelper/lang/en_us.json`
- 模块主体：`src/main/java/me/matl114/hacks/modules`
- 项目说明与模块文档：`README.md`、`agent/`

模块大体包括挖掘、聊天、对战、渲染、物品栏、移动、网络、交互、粘液科技、物品模型、任务和其他增强功能。

## 通用协作要求

- 默认使用简体中文沟通。
- 保持输出直接、具体，不写无关解释。
- 不主动回退用户已有改动。
- 不主动扩大任务范围；用户没有要求的重构、检查、提交、运行服务都不要做。
- 修改代码时优先贴合当前项目已有结构、命名、配置链和模块组织方式。

## 任务类型要求

### 1. 代码生成任务

适用范围：新增或修改 Java 代码、模块代码、配置声明、命令、GUI、Mixin、工具类、脚本支持等。

要求：

- 只需要把代码写出来。
- 不需要主动编译。
- 不需要主动运行测试。
- 不需要主动运行格式检查、风格检查、lint、checkstyle 或类似检查。
- 不需要为了验证而执行 Gradle 任务。
- 写完后直接交付，等待用户反馈；用户会自行检查、运行、调整或继续提出修改意见。

### 2. 语言文件补全任务

适用范围：补全或调整 `src/main/resources/assets/slimefunhelper/lang/en_us.json` 中的语言 key、模块显示名、配置项文案、枚举文案、命令文案等。

要求：

- 贴近当前语言文件已有结构、key 命名方式、分组顺序和文案风格。
- 优先参考同模块、同配置类型、同 UI 位置附近的现有文案。
- 不需要主动编译。
- 不需要主动运行 JSON 检查、格式检查、lint 或其他检查。
- 写完后直接交付，等待用户反馈。

### 3. 代码理解任务

适用范围：解释项目结构、模块关系、配置链、事件流、数据流、状态变化、Mixin 注入关系、语言 key 来源、运行路径等。

要求：

- 以只读分析为主。
- 优先说明模块关系、数据流向、状态变化和作用域。
- 不默认从单个函数或代码细节展开，除非用户明确要求。
- 不主动修改文件。
- 不主动运行编译、测试或检查，除非用户明确要求。

### 4. 其他任务

适用范围：不属于代码生成、语言文件补全、代码理解的任务。

要求：

- 无额外项目要求。
- 按用户当前明确指令执行。
- 如果任务目标、范围或交付物不清楚，先询问用户。