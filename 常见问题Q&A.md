# 常见问题
本文档总结了部分玩家常见的问题

## 基础问题

### 客户端指令是什么
  
客户端指令前缀是!!或者/!!,

建议使用/!!防止指令误发送泄露关键信息

两个前缀等价，下文中统一以!!指代

输入!!help 可以获取指令帮助

### 如何打开ClickGui
    
使用右Alt

该快捷键可以在Tasks分类中的ClickGui模块中修改

### 快捷键怎么设置,如何使用"按住启用"的快捷键
快捷键设置方式类似masa的生电模组,支持多个key组合成复合快捷键

快捷键设置右侧的T按钮为切换triggerOnBindRelease(是否在松开的时候再次触发快捷键)

当然,由于避免在打字的时候误触快捷键,大部分快捷键对应的功能默认在打开屏幕的时候不生效,该选项可以在Tasks分量中的**Modules**配置

## 配置方面

### 如何导入配置，配置怎么调

配置管理指令为!!config,基础用法:

打开配置导出和导入用的文件夹 游戏文件夹/slimefunhelper/config/
>> !!config openfolder
  
加载参数, 其中加载根目录是 游戏文件夹/slimefunhelper/config/
>> !!config load 我的参数名.nbt

保存参数
>> !!config save 我要保存的名称

加载参数中单独几个模块
>> !!config loadmodule 我的参数名.nbt 模块1 模块2

保存单独几个模块的参数
>> !!config savemodule 我要保存的名称 模块1 模块2

重置参数到出厂设置
>> !!config reset 配置子分类id 配置项路径
>> !!config resetall 配置子分类id --confirm

重置单独几个模块的参数到出厂设置
>> !!config resetmodule 模块1 模块2

### 如何找到配置文件的文件位置

运行时读取的配置文件在 游戏文件夹/config/sfhelper-configs/下面以多个yml文件存储

**如果你想要分享你的参数,请使用上述指令保存之后再分享.而不是直接压缩这个文件夹**

### 游戏目录下面的slimefunhelper/文件夹是干啥的

该目录下存了当前的部分运行时数据

除了slimefunhelper/config/下的文件，你不应该移动或者随意分享其他任意位置的文件，否则**后果自负**


## 核心功能方面

### 要不要开via,开了via怎么调参数
建议开Via低版本1.20.5-1.20.6，这样不仅可以提升功能强度,还有神秘小特性用

但是如果你想使用高版本直连,也是没有问题的

我们的release中提供了使用via和noVia的两份参数

或者用户可以使用指令

自动纠正部分参数至直连Grim的参数
>> !!preset ac_grim

自动纠正部分参数至使用Via后Grim的参数
>> !!preset ac_grim_legacy

自动纠正部分参数至无反服参数
>> !!preset hacking

### 参数调废了怎么办
可以选择打开Extra分类中的IQBoost模块

或者详细阅读章节:**如何导入配置，配置怎么调**

中的指令使用方法, **养成调完就保存的好习惯**,同时可以通过逐个加载有改动的模块来控制变量测试问题在哪里

### 怎么调鞘翅飞行
鞘翅相关功能主体在ElytraExtra模块中，该模块提供了以下几类设置
- 单独的小功能
- 玩家如何起飞
  - 玩家可以通过若干种方式进入"滑翔状态", 例如双击跳跃键(原版),或者一键起飞(模块调用)
  - 玩家有若干种方式维持"滑翔状态": 1. 原版鞘翅飞行, 2. 鞘翅甲飞, 3. 无限耐久鞘翅
  - 不管玩家通过什么方式维持的"滑翔状态",玩家的运动行为都是一致的
- 玩家是否受烟花增益
  - 在某种角度,单个火箭和多个火箭效果等价
  - 模块计算了玩家是否受火箭增益,维护了玩家放火箭的计时器,并提供了简易的火箭动量增幅功能

以下是几个常用模块
- ElytraFlight
  - 该模块会给**在滑翔状态** 且 **存在烟花增益/存在动力**的玩家以动量控制的能力
  - 玩家可以通过WASD或者其他模块完全控制运动行为
- ElytraGrimAcc
  - 该模块通过Grim拉回工作,在本地和未安装Grim的服务器不工作
  - 该模块可以向**在滑翔状态**的玩家提供速度增益

### 怎么调追人
追人模块为ElytraBot,该模块通过调用ElytraFlight或者Flight(无反作弊飞行)进行飞行移动,玩家需要打开对应的模块才可以使用追人功能

追人功能可以选择玩家操作是否可以覆盖追人操作，允许你在追人运行的时候手动接管

追人模式有三种模式,可以配置快捷键切换模式，不同模式下有不同的配置选项

**重锤模式下会自动切锤**, 玩家不需要调整任何类似"秒切重锤", "秒切武器"的功能

**长矛模式下不会自动使用长毛**, 玩家可以按住右键或者使用AutoUse模块

### 为啥甲飞飞起来是鞘翅
可能装了其他模组的起飞自动切翅功能

关闭其他模组的切翅功能或者 勾选 鞘翅甲飞: 起飞时强制切甲

### 怎么配置喷药
我们使用InteractManager模块进行喷药操作

该模块提供了!!useitem !!attack !!holduse 三个指令

分别提供不同的自动交互服务

获取帮助:
>> !!iman help useitem
>
>> !!iman help attack
> 
> > !!iman help holduse

玩家可以使用BindCmd模块将配置好的指令绑定到一个快捷键上,当玩家触发该快捷键时,会自动输出文本,触发执行指令,进而实现自动喷药

上述帮助指令中提供了详细的指令解释和示例,如果有不明白的，可以点击拷贝键将指令拷贝去问豆包

## 杂项功能方面

### 怎么加Friend
使用指令!!friends add <玩家名字> [可选:玩家昵称] 添加好友

或者在TargetSelector模块中配置添加好友快捷键

在TargetSelector中可以编辑好友列表

### 怎么单模块绑定多个快捷键/怎么使用自动喷药等
这些杂项功能都可以使用指令完成

在Tasks分类中查找BindCmd模块,该模块允许玩家通过快捷键触发玩家预设的指令

上述功能都可以通过指令实现,其中, 输入!!iman help useitem获取使用物品指令的教程和相应示例

### 渲染功能怎么关
大部分渲染功能在Render分类中

挖掘进度渲染在MineExtra模块中, 

攻击目标渲染在Attack模块中

箱子名字渲染在ChestHistory模块中

在上述模块中找到对应功能关闭即可

### 找不到模块怎么办
使用ClickGui界面的搜索栏,支持拼音搜索,可以搜索配置项名称或者模块名称

### 不小心开了神秘模块怎么办
那就找找开了什么模块,

一般模块被快捷键打开的时候会在聊天框提示

## 其他方面

### 是否会改进Gui
总有一天...

### 有bug怎么办
有bug可以找作者或者其他懂的人, 但是建议先自行分析一下是否有其他模组干扰